# This file is intentionally empty.
# Each sub-project manages its own environment:
#   backend/.env.example  — NestJS (DATABASE_URL, ANTHROPIC_*, FRONTEND_URL)
#   frontend/.env.example — Vite   (VITE_API_URL)
